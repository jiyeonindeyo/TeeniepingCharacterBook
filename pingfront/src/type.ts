export type Ping = {
    id?: number;
    name: string;
    season: number;
    tool?: string;
    skill?: string;
    image?: string;
}

export type User = {
    userName: string;
    password: string;
}